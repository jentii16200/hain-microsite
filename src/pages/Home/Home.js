import React, { useEffect, useState } from 'react';
import { Center } from '@chakra-ui/react';
export const Home = (props) => {
    return(
        <>
            <Center>
                TEXT SAMPLE
            </Center>

        </>
    );
};